<section class="py-5 text-center container">
  <div class="row py-lg-5">
    <div class="col-lg-6 col-md-8 mx-auto">
      <h1 class="fw-light p-2 h2">トピックス一覧</h1>
      <br>
      <p>
      キーワード検索はタイトルと本文の中を「ひらがな・カタカナ」検索します<br>
      <br>
      匿名希望は名前なしの判定なので検索不可です<br>
      <br>
      </p>
      <hr>
      <div class="justify-content-center align-items-center">
        <%= search_form_for @q, url: admin_topics_path, method: :get do |f| %>
          <p>
            <%= f.label :title_or_content_cont, "キーワード検索", class:"col-sm-4 col-form-label" %>
            <%= f.text_field :title_or_content_cont, class:"col-sm-4" %>
          </p>
          <p>
            <%= f.label :user_screen_name_cont, "ハンドルネーム名", class:"col-sm-4 col-form-label" %>
            <%= f.text_field :user_screen_name_cont, class:"col-sm-4" %>
          </p>
          <p>
            <%= f.label :user_name_cont, "投稿者名", class:"col-sm-4 col-form-label" %>
            <%= f.text_field :user_name_cont, class:"col-sm-4" %>
          </p>
          <p>
          <%= f.submit "検索", class:"btn btn-custom" %>
          </p>
        <% end %>
      </div>
      <div class="align-items-center">
        <p><%= link_to "検索結果をリセット", admin_topics_path, class:"btn btn-custom" %></p>
      </div>
    </div>
  </div>
</section>

<div class="container">
  <div class="table-responsive">
    <table class="table caption-top">
      <caption>ソート項目は各ボタンを押してください</caption>
      <thead class="text-center align-middle custom-thead">
        <tr>
          <th></th>
          <th>タイトル</th>
          <th>本文</th>
          <th>投稿者名</th>
          <th>ハンドルネーム</th>
          <th><%= sort_link(@q, :created_at, "作成日時", hide_indicator: true, class: "btn btn-light") %></th>
          <th>画像の有無</th>
        <tr>
      </thead>
        <tbody class="align-middle">
          <% @topics.each do |topic| %>
            <tr>
              <td class="text-center"><%= link_to "詳細", admin_topic_path(topic), class:"btn btn-custom" %>
              <td class="text-center"><%= truncate(topic.title, length: 15) %></td>
              <td><%= truncate(topic.content) %></td>
              <td class="text-center"><%= truncate(topic.user.name, length: 15) %></td>
              <td class="text-center"><%= truncate(topic.user.screen_name, length: 15) %></td>
              <td class="text-center"><%= topic.created_at.strftime('%Y/%m/%d') %></td>
              <td class="text-center"><%= topic.topic_images.attached? ? "あり": "なし" %></td>
            </tr>
          <% end %>
        </tbody>
    </table>
  </div>
</div>

<%= paginate @topics %>